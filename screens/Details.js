import {
  Text,
  View,
  SafeAreaView,
  Image,
  StatusBar,
  FlatList,
} from "react-native";

import {
  CircleButton,
  RectButton,
  SubInfo,
  DetailsDesc,
  DetailsBid,
  FocusedStatusBar,
} from "../components";

const Details = ({ route, navigation }) => {
  console.log(route);
  return <Text>Detailsr</Text>;
};

export default Details;
